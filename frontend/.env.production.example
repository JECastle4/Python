# Production environment configuration TEMPLATE
# Copy this file to .env.production and set your actual production API URL
# 
# IMPORTANT: .env.production is git-ignored and must be configured per deployment
# 
# Steps to deploy:
# 1. Copy: cp .env.production.example .env.production
# 2. Set VITE_API_BASE_URL to your actual production API URL
# 3. Example: VITE_API_BASE_URL=https://api.yourdomain.com
#
# Leaving this empty will cause the app to default to http://localhost:8000
# which will NOT work in production.
VITE_API_BASE_URL=
